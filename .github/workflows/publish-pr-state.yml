name: Live PR State (replaced)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Replaced by Publish Dashboard. This workflow is intentionally manual-only."
